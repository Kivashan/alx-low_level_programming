#include "main.h"

/**
 * mod - returns the remainder from the division of two integers
 * @a: integer value
 * @b: integer value
 *
 * Return: the remainder of the division between two integers
 */

int mod(int a, int b)
{
	return (a % b);
}
